export interface SliderSettings {
    dots: boolean;
    autoplay: boolean,
    infinite: boolean;
    fade: boolean;
    arrows: boolean;
    autoplaySpeed: number;
    speed: number;
    slidesToShow: number;
    slidesToScroll: number;
}